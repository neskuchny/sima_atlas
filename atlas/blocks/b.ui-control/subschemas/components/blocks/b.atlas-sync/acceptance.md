# b.atlas-sync — acceptance

- [ ] **A1 (logic flow).** Компонент рендерится без runtime-error на cold-load.
- [ ] **A2 (sync).** Изменение в parent-блоке b.ui-control триггерит ре-рендер этого компонента.
- [ ] **A3 (scenario).** Golden e2e flow для родительской фичи проходит зелёным.
