# b.search — archived orphan

This block was created by LLM chat-extraction (confidence=1) but never
accepted into graph.json — it existed only as a loose directory.
Moved to `archive/orphan-blocks/b.search/` during Phase I (Sima fixes Sima)
to keep the active block tree honest. Restore with:
  git mv archive/orphan-blocks/b.search atlas/blocks/b.search
then accept it through the proposal flow or add it to graph.json.
