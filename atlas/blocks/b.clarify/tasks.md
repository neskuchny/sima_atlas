# b.clarify — tasks

## PR1 — протокол вопроса (сделано)
- [x] T1. `clarify_block.mjs`: схема с tri-state `[inconclusive, clear, questions]`, inconclusive первым.
- [x] T2. Порог «спрашивать против предположить» по четырём осям в системном промпте.
- [x] T3. `normalizeQuestions` отбраковывает ярлык-вместо-вопроса и вопрос без выбора кодом, а не просьбой.
- [x] T4. Сортировка вопросов по цене поздней находки (scope → security → behaviour → acceptance).
- [x] T5. `facts_verified` и `assumptions` как обязательные поля схемы.
- [x] T6. Форсирование `inconclusive` при mock-провайдере.

## PR2 — маркеры неопределённости (сделано)
- [x] T7. `findMarkers` / `blockMarkers` — парсинг маркеров неопределённости по файлам контракта. Синтаксис маркера намеренно не приводится здесь дословно: детектор не отличает объявление от цитаты, и пример в собственном контракте блока сработал бы как настоящий открытый вопрос (поймано первым же прогоном валидатора — см. narrative).
- [x] T8. `validate_clarifications.mjs` — done-гейт, review-warning, idea/wip-info.
- [x] T9. `resolveMarker` — замена маркера ответом без остатка противоречивого текста.
- [x] T10. `appendAnswers` — append-only Q→A лог с датой сессии.
- [x] T11. Регистрация валидатора и selftest'а в nightly.

## PR2.5 — направление «человек → модель» (R-8.08, сделано)
- [x] T18. `block_meaning.mjs`: чтение секции траектории из mission.md (русские и английские заголовки), вырезание её из миссии, чтобы промпт не нёс текст дважды.
- [x] T19. Промпт исполняющего агента: секция траектории как правило выбора между зелёными вариантами, а не как задача на сейчас; при отсутствии — требование записать выбор в допущения.
- [x] T20. Step 0 в промпте агента: `understanding.md` с фиксированными заголовками до первой строки кода.
- [x] T21. `clarify_block.mjs`: обязательное поле `operative_frame` — модель называет рамку, в которой читает блок; на mock — `null`, а не выдуманная рамка.
- [x] T22. `validate_meaning.mjs` — отчёт с условиями повышения до гейта и снятия; регистрация в nightly.
- [x] T23. Траектория для самого b.clarify. Для остальных блоков — нет: куда движется блок, знает оператор, а выдуманная агентом траектория была бы ровно тем, против чего блок создан.

## PR2.6 — пауза между объявлением и кодом (R-8.09, сделано)
- [x] T26. `frameGate`: состояние выводится из журнала `frame_reviews.jsonl` и файлов на каждый вызов — none / awaiting / confirmed / corrected / stale. Подтверждение привязано к тексту объявления и отпечатку контракта; галочки в acceptance.md отпечаток не меняют.
- [x] T27. `run_block_implementation.mjs` двухфазный по умолчанию: фаза объявления (только understanding.md, проверка, что агент ничего больше не тронул), ожидание (агент не запускается), фаза реализации (рамка как данность, переписывать нельзя). `--frame-review=skip` — старый однофазный режим, пишется в checks.log.
- [x] T28. Поправка оператора попадает в промпт следующего объявления дословно, вместе с рамкой, которую она поправляет.
- [x] T29. Объявление на языке миссии (`operatorLanguage`, `ATLAS_OPERATOR_LANG`).
- [x] T30. Демон: блок, ждущий человека, — `awaiting-frame`, без верификатора, без продвижения, не провал; `--frame-review skip` для полностью автономной ночи.
- [x] T31. Панель «Обзор»: «✓ Верно — писать код», «✎ Не так» с полем поправки, «подтвердить, не запуская»; состояния шлюза словами; обновление раз в 5 с.
- [x] T32. `/runs/start` не запускает агента, пока рамка ждёт ответа, и говорит об этом; кнопка «Отправить агенту» передаёт client.

## PR3 — канвас (не начато)
- [ ] T12. Подсветка блока с открытыми маркерами на графе.
- [ ] T13. Панель вопросов: вариант + последствие, ответ одним кликом.
- [ ] T14. Вторая полоса здоровья блока — «качество формулировки» отдельно от «состояния приёмки».
- [ ] T15. Отображение `assumptions` как видимого долга на карточке блока.
- [x] T24. Рамка агента на панели «Обзор» блока — оператор видит «Treating this as», допущения, устаревание и траекторию, не открывая файл. Данные разбирает сервер той же функцией, что и ночной отчёт; канвас только рисует.
- [ ] T25. Метка на самом узле графа, когда объявление устарело или неполно — чтобы расхождение было видно без клика. Нужна сводка по всем блокам в потоке состояния канваса.

## PR4 — измерение (не начато)
- [ ] T16. KPI-5: ряд «вопросов на блок» по времени из `clarifications.md`.
- [ ] T17. KPI-6: доля вопросов, получивших содержательный ответ.
