# b.user-docs-generator — KPI

> **Статус замера (R-8.05, аудит самоконтроля).** Файл содержал `Сейчас: ✗`
> по всем пунктам — шаблон, не обновлённый после перевода блока в `done`.
> Ниже: измеренное помечено ✓/✗ с доказательством, неизмеренное — честно
> «не измерено». KPI-1 измерен и **не выполнен**: блок в `done`, но своего
> профильного результата не произвёл ни разу.

- **KPI-1 (coverage)**: 100% блоков с `layer ∈ {user, front}` или `user_facing: true` имеют свежий `docs/end-user/<block>.md` (т.е. hash источников совпадает с hash в `_meta/<block>.json`). Сейчас: ✗ (измерено) — `atlas/docs/end-user/` пуста (0 файлов, `_meta/` тоже), при двух user-facing блоках в графе: `b.ui-control` (front, wip) и `b.product-dashboard` (front, idea). Покрытие 0/2.
- **KPI-2 (idempotent)**: regen без изменений источников даёт diff = пустой (байт-в-байт идентичный файл). Сейчас: не измерено (нечего мерить: сгенерированных документов нет).
- **KPI-3 (cost cap)**: одна регенерация одного блока ≤ $0.03 (LLM tokens); полный regen всех user-facing блоков среднего проекта (8 блоков) ≤ $0.25. Сейчас: не измерено (нечего мерить: сгенерированных документов нет).
- **KPI-4 (no jargon)**: 0 случаев технических терминов (`module`, `component`, `endpoint`, `prop`, `state`) в финальном markdown — LLM-prompt + post-validation. Сейчас: не измерено (нечего мерить: сгенерированных документов нет).
- **KPI-5 (drift detection)**: при ручной правке `<block>.md` без `LOCKED: true` — pre-commit hook просит подтвердить либо отменить (warn, не fail). Сейчас: не измерено (нечего мерить: сгенерированных документов нет).
- **KPI-6 (latency)**: один блок ≤ 30 секунд (только LLM, без Playwright); с Playwright ≤ 60 секунд. Сейчас: не измерено (нечего мерить: сгенерированных документов нет).
- **KPI-7 (graceful degradation)**: если Playwright не настроен — текст всё равно валидный, без `[broken image]`. Сейчас: не измерено (нечего мерить: сгенерированных документов нет).
