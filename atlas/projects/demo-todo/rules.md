# Rules — Demo TODO

1. Один React SPA на фронте; никакого SSR в MVP.
2. Один бэкенд-сервис; никакого микросервисного зоопарка.
3. БД одна — Postgres; не использовать Mongo / DynamoDB / другие хранилища.
4. LLM-категоризатор зависит от `b.llm-gateway` Атласа (Anthropic / Google / mock); не подключать прямого SDK.
5. Авторизация — отложена в PR5+ (сейчас single-tenant local-only).
6. Любой блок переходит в `done` только когда пройдены acceptance + KPI + e2e на golden-сценарии «добавить задачу → отметить → удалить».
