# b.todo-ui — acceptance

- [ ] **A1 (logic flow).** Все 5 user-stories реализованы; e2e тест на каждую существует.
- [ ] **A2 (sync с API).** Если контракт `b.todo-api` меняется — UI получает 4xx и показывает понятную ошибку (не пустой экран).
- [ ] **A3 (scenario).** Golden flow «add → toggle → delete» проходит без ручных действий.
