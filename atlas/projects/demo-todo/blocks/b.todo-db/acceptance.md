# b.todo-db — acceptance

- [ ] **A1 (logic flow).** Schema diff между drizzle и реальной БД пуст (sync).
- [ ] **A2 (idempotent migration).** Прогон migrate дважды → 0 ops второй раз.
- [ ] **A3 (scenario).** Golden flow с реальной БД проходит за < 200ms.
