# Roadmap (now)

1. **b.db** — зафиксировать схему Atlas DB и статусы файлов.
2. **b.core-sync** — валидации stack/rules/depends/provides + отчёт sync.
3. **b.agent-orchestrator** — единый context-pack + протокол обновления блока.
4. **b.ui-control** — визуальная панель статусов, кнопки sync/review/implement.
